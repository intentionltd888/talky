# Talky 測試清單

裝好、照這份走一遍，卡住的地方寫一句話回報。**不用懂技術**，每一條都只要「做一個動作、看一個結果」。

## 0. 先分你是哪一種

| 你的電腦 | 怎麼做 |
|---|---|
| **沒裝過 Hearby**（同一家的會議記錄 app） | 直接照下面走。第一次打開會下載語音模型（約 1.5GB，一次性）。 |
| **裝過 Hearby** | 模型會直接借用 Hearby 已下載的那份，不用再下載。精靈第 4 步（試講）如果 Hearby 正在跑，會多一顆「關掉它的輸入法」——**按一下**。Hearby 的會議錄音、逐字稿完全不受影響，只是它不再搶右 ⌘。 |

需求：Apple 晶片（M1 以上）的 Mac、macOS 14 以上。

## 1. 安裝（2 分鐘）

1. 打開 `Talky-<版本>.dmg`，把 Talky 拖進 Applications。
2. 從 Applications 打開 Talky（本版經 Apple 公證，不會有安全警告；有的話截圖回報）。
3. Dock 會出現深底白米字的圖示；右上角選單列也有一顆小米字（就緒＝空心）。

## 2. 第一次打開：五步精靈（3 分鐘）

跟著走就好。要親手做的只有三件：按「允許」、去系統設定開一個開關、講一句話。

- 歡迎頁按「開始設定」：聽寫模型就在背景開始下載（頁尾有進度條），不用等。
- 第 1 步「麥克風」→ 按「允許」。
- 第 2 步「輔助使用」→ 按「去開啟」，系統設定會開在那一頁：找到 Talky、把開關打開（可能要輸入 Mac 密碼），回來會自動打勾。**這一步不能跳：沒開的話連按右 ⌘ 完全不會有反應。** 開了卻沒打勾：按「清掉舊記錄再開一次」。
- 第 3 步「怎麼整理你講的話」：兩張大卡並排——**用我的訂閱**（ChatGPT 或 Claude；登入了就自動選它；都沒登入時按這張卡＝直接接 ChatGPT：沒有 Codex 會自己下載 90MB、然後跳瀏覽器登入、登入完馬上綁定）／**用本機模型**（不用帳號）。「延伸選項」才看到全部（Claude 那列的安裝／登入也在這），每顆有「測試」，成功寫「測試成功」。
- 第 4 步兩關：①「講一句試試」——**⌘⌘ 超快速按兩下**、說「今天天氣不錯」、**再超快速按兩下** → 字出現 → 自動進第二關 ②「再來一段，隨便聊」——照範例講一段亂的（列點、改口）→ 字出現＝「你成功了，歡迎使用」，2 秒後自動到最後一頁。不想按鍵盤按「用按的」；「夠了，下一步」可跳過第二關。
- 第 5 步「好了」：五格都打勾就按「完成」；缺的那列有按鈕。開機自啟與狀態燈預設開。

## 3. 每天用（重點）

在任何文字框（備忘錄、瀏覽器、LINE、終端機）：**連按兩下右 ⌘ → 講 → 再按一次**。

要看到的：

1. 按下去：螢幕下方出現面板，米字在呼吸、有音量槽、邊講邊出字。
2. 再按一次：面板寫「整理中，用你的 ___」＋「約 N 秒」。
3. 字貼進游標處，面板寫「已貼上」，**0.9 秒後自己消失**（沒消失＝bug，回報）。
4. 貼不進去的地方（例如密碼欄）：面板寫「已複製，按 ⌘V 貼上」，你自己貼。
5. 連按右 ⌘ 完全沒反應：點 Dock 圖示，狀態視窗下面那條會告訴你缺什麼（多半是輔助使用），按它的按鈕開完就好，不用重開 app。

## 4. 逐項檢查（30 分鐘，勾完回報）

| # | 做什麼 | 應該看到 |
|---|---|---|
| 1 | 在備忘錄、Chrome 網址列、終端機各口述一句 | 三個地方都貼得進去 |
| 2 | 講一句中英夾雜：「這個 feature 禮拜五 deadline」 | 英文原樣保留、不會被翻譯 |
| 3 | 講到一半改口：「三點，啊不對，四點」 | 只留四點 |
| 4 | 講簡體字習慣的詞 | 貼出來一律繁體、全形標點 |
| 5 | 講一段 1 分鐘的長話 | 貼上時間不超過 20 秒；沒被砍尾 |
| 6 | 點 Dock 圖示 | 開狀態視窗；上面有「試講一句」與備忘錄 |
| 7 | 狀態視窗的備忘錄 | 每句口述都在，點一下可複製 |
| 8 | 設定 → 一般 → 外觀：切「淺色」「深色」「跟系統」 | 所有視窗與面板一起換色 |
| 9 | 設定 → 一般 → 重設快捷鍵：改成右 ⌥ | 之後連按兩下右 ⌥ 才會觸發 |
| 10 | 設定 → 一般 → 常用詞：加一個人名，再口述那個名字 | 辨識與整理都認得 |
| 11 | 設定 → 進階：三顆可見（ChatGPT／Claude／不用帳號），點整列切換，再按「測試」 | 已連接的要在幾秒內回一句（Claude 約 3 秒、ChatGPT 約 8 秒）；沒裝 Claude 的按「安裝」會在 app 裡裝好（不開終端機），裝好自動接登入 |
| 12 | 設定 → 進階：按「更多選項」展開，自填 OpenAI 相容端點或 Anthropic 金鑰 → 儲存 → 測試 | 能回一句；金鑰不會出現在診斷檔（這條只給有金鑰的人測，一般人跳過） |
| 13 | 把整理大腦切成「不整理」再口述 | 面板寫「已貼上（未整理）」，字是原稿但已繁體化 |
| 14 | 拔掉網路（或關 Claude）再口述 | 自動退到本機模型或原稿，面板照實說走了哪條，不會卡死 |
| 15 | 合上電腦再打開、或睡醒後口述 | 引擎自己回來，不用重開 app |
| 16 | 設定 → 一般 → 「匯出診斷檔到桌面」 | 桌面出現 `Talky診斷_日期.txt` |
| 17 | 裝過 Hearby 的人：兩個 app 同時開，連按右 ⌘ | 只有 Talky 出面板；Hearby 錄會議照常 |
| 18 | 結束 Talky（選單列米字 → 結束）再從 Applications 打開 | 不會再跑精靈，直接就緒 |
| 19 | 沒選過整理方式的新機（或 `defaults delete ltd.intention.talky polishMode` 後重跑精靈） | 第 3 步自動選對：有 ChatGPT 登入選它、否則 Claude、否則內建；理由那行寫得對 |
| 20 | 把整理大腦選 Claude Code，拔網路後口述 | 面板寫整理中→貼上；狀態視窗顯示走了本機模型（有內建模型的機器），沒有內建模型就「已貼上（未整理）」 |
| 21 | 在密碼欄（例如系統設定的密碼框）口述 | 不會自動貼，只進剪貼簿，面板寫「已複製」 |
| 22 | 先在系統設定把 Talky 的輔助使用關掉，再打開（app 不重開） | 狀態視窗下面那條先出現「沒開輔助使用」，開回去後 2 秒內消失，連按右 ⌘ 又有反應 |
| 23 | 設定 → 進階最上面「複製這段貼給你的 AI」，貼到你的 Claude 或 ChatGPT | 它會照步驟帶你，最後要你親手連按右 ⌘ 驗收 |
| 24 | 有 Claude 訂閱但命令列從沒登入過的人：設定 → 進階 → Claude 那列按「登入」 | 瀏覽器自己打開、登入按允許（網頁給代碼就貼回 Talky 那格）→ 那列自動變「已連接（Max／Pro，opus）」，不用開終端機 |
| 25 | Claude 那列按「測試」 | 幾秒內回一句；狀態寫的模型是 opus |

## 5. 怎麼回報

一則訊息三樣東西：

1. 一句話：做了什麼、看到什麼（例：「第 3 條，改口後兩個時間都留著」）。
2. 桌面那份 `Talky診斷_日期.txt`（設定 → 一般 → 匯出診斷檔到桌面）。
3. 有畫面問題就截圖。

## 5b. 從零開始的朋友（終端機派）

`bash scripts/setup.sh` 會檢查並備齊：Talky.app 裝進 Applications、Claude Code（官方腳本）、ChatGPT 桌面版（開下載頁）、Ollama（有 Homebrew 就裝）＋模型，最後印體檢。不會終端機的人不用管這段，開 app 照精靈走就好。

## 6. 已知還沒做（不用回報）

- 任意組合鍵錄製（現在只有右 ⌘／右 ⌥／fn 三選一）。
- 「檢查新版」與自動更新（新版會直接給你新的 DMG）。
- 邊講邊出字是每 1.4 秒重轉，長口述放開後要多等一下。
- 密碼欄與安全輸入框一律只進剪貼簿（刻意的）。
